{
  "name": "smart-agriculture-copilot-root",
  "version": "1.0.0",
  "description": "Smart Agriculture Copilot - Full Stack Root",
  "private": true,
  "scripts": {
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "dev": "npx -y concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start": "npm --prefix server run start",
    "build": "npm --prefix client run build"
  }
}
